"use client";
import { FormEvent, useState } from "react";
export default function AdminLogin() { const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setError(""); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) }); if (response.ok) location.href = "/admin"; else setError((await response.json()).error ?? "Gagal masuk."); setLoading(false); } return <main className="admin-login"><form onSubmit={submit}><p>JCP / ADMIN</p><h1>Kelola situs.</h1><label>Password admin<input type="password" autoFocus required value={password} onChange={(e) => setPassword(e.target.value)} /></label>{error && <small>{error}</small>}<button disabled={loading}>{loading ? "Memeriksa…" : "Masuk"}</button><a href="/">Kembali ke situs</a></form></main>; }
